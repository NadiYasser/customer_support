"""IT support agent (M3).

For merchants reporting website trouble. Tool: create_ticket(...) against a
swappable TicketRepository (mocked now, real Jira/Zendesk later).
"""
# TODO(M3): implement the IT support agent + create_ticket tool.
