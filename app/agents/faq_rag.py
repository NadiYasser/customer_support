"""FAQ / Policy agent — the RAG path (M2).

Retrieves relevant chunks from the knowledge base (Chroma) and answers the
question grounded in that retrieved text. Tool: search_kb(query).
"""
# TODO(M2): implement the RAG agent using app.rag.retriever.
