"""Supervisor / router node (M3).

Reads the latest customer message and decides which specialized agent should
handle it (faq_rag, tracking, refund, modify, it_support). This is the
orchestration heart of the system.
"""
# TODO(M3): implement the routing decision (intent -> agent name).
