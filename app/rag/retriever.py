"""Retriever (M2).

Embeds the query (Gemini) and returns the top-k most similar chunks from the
Chroma collection. The FAQ/RAG agent grounds its answer in these chunks.
"""
# TODO(M2): implement top-k similarity search over the Chroma collection.
